/**
 * Write a function that, given a sentence like the one used as an example below, along with the position of an opening parenthesis,
 * finds the index of the corresponding closing parenthesis.
 * <p/>
 * Example:
 * input sentence: "Sometimes (when I nest them (my parentheticals) too much (like this (and this}{))) they get confusing."
 * input opening_paren_index: 10 (position of the first parenthesis)
 * output: 79 (position of the last parenthesis)
 */

 const parenthesis = () => {

}

console.log(parenthesis());